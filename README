Hello there !
Nothing much to be found here.

This is a sandbox for Cynergiae.

--------------------------------------------------------------------------------

INSTALL NOTES
=============

CD to your project directory and then clone the repo :
git clone git@github.com:Nutellove/Cynergiae.git cynergiae

Execute install_symfony.sh to install sf2 vendors.


Later, you might execute update_symfony.sh to pull changes from
vendors' repositories.

--------------------------------------------------------------------------------

Git CheatSheet
--------------

git add file1 file2 folder1 etc
git commit -a -m 'Commit comment'
git push origin master
git pull origin master
